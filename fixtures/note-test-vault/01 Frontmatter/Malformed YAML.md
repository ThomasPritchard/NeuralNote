---
tags: [qa/frontmatter, broken
owner:
  name: Test
---
# Malformed YAML body remains visible

This body must never disappear. NeuralNote should show an explicit frontmatter parse error and the offending raw block.
